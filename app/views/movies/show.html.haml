%strong= @movie.name
- @shows.each do |show|
  .card.margin-10
    .card-body{onclick: "window.location='/shows/#{show.id}'", style: "cursor: pointer;"}
      %strong time
      %p= show.time
      %strong fare
      %p= show.fare
