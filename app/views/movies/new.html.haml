%h1= t('Create a new movie')
= form_for(@movie) do |f|
  .form-group
    = f.label :name
    = f.text_field :name, autofocus: true, class: 'form-control'
  .form-group
    = f.label :genre_ids, "Genres"
    = f.collection_check_boxes :genre_ids, @genres, :id, :title, class: 'form-control'
  .form-group
    = f.submit 'Create', class: 'btn btn-primary'